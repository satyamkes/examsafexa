# Copy this file to .env.local and fill in real values

# Maps (Phase 2 — interactive center picker)
NEXT_PUBLIC_MAPBOX_TOKEN=

# Database (Phase 2 — auth and exam/center selection)
MONGODB_URI=

# Auth (Phase 2 — NextAuth.js credentials provider)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=

# Email (Phase 3 — parent/guardian safety notifications)
RESEND_API_KEY=

# Real-time (Phase 3+ — live location sharing)
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
